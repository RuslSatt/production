import { CounterScheme } from './model/types/counterScheme';
import { counterActions, counterReducer } from './model/slice/counterSlice';
import { Counter } from './ui/Counter';

export { Counter, counterReducer, counterActions, CounterScheme };
